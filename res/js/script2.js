function changeItem1(obj){	if( obj.value == "" ){		obj.parentNode.parentNode.parentNode.style.boxShadow = "none";	}else{		obj.parentNode.parentNode.parentNode.style.boxShadow = "0 0 0 15px #f18bb2";	}}function changeItem2(obj){	if( obj.value == "" ){ 		obj.parentNode.parentNode.parentNode.style.boxShadow = "0 0 0 1px #a60241";	}else{		obj.parentNode.parentNode.parentNode.style.boxShadow = "0 0 0 15px #a60241";	}}function navigation(){	var $elem = $(".nav_outer"), //表示の操作をするオブジェクト(フッター)	$content = $("#nav"), //表示を変更する基準となるオブジェクト	$win = $(window); //windowオブジェクト		var contentTop = 0; //表示変更をする基準点	$win		.load(function(){				 updatePosition();				 update();		})		.resize(function(){				 updatePosition();				 update();		})		.scroll(function(){				 update();		});	// HTMLが動的に変わることを考えて、contentTopを最新の状態に更新します	function updatePosition(){		contentTop = $content.offset().top + $elem.outerHeight();	}	// スクロールのたびにチェック	function update(){		// 現在のスクロール位置 + 画面の高さで画面下の位置を求めます		if( $win.scrollTop() + $win.height() > contentTop ){			$elem.addClass("static");		}else if( $elem.hasClass("static") ){			$elem.removeClass("static");		}	}}function roll_over(){	/* roll over */	$(document).on('mouseenter','.imgover',function(){		$(this).attr('src', $(this).attr('src').replace('_off', '_on'));	});	$(document).on('mouseleave','.imgover',function(){		$(this).attr('src', $(this).attr('src').replace('_on', '_off'));	});}function smooth_scroll(){	/* smooth scroll */	$("a[href^=#]").click(function(){		var Hash = $(this.hash);		var HashOffset = $(Hash).offset().top;		$("html,body").animate({ scrollTop: HashOffset}, 'slow','swing');		return false;	});}$(function(){	roll_over();	navigation();	smooth_scroll();	/*lazyload*/	$('.lazy, .bgLoad').lazyload({		threshold: 250,		effect: 'fadeIn',		effectspeed: 800	});	/*	$("a.colorbox").colorbox({		returnFocus: false	});	*/	$('a.swipe').photoSwipe({		hideAnimationDuration:0,		showAnimationDuration:0,		animationDuration:0	});});